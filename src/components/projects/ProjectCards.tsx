import { FC } from 'react';
import ProjectCard from './ProjectCard';

const ProjectsCards: FC = () => {
    return (
        <>
            <ProjectCard projectName={'aaaa'} projectDescription={'aaaa'} projectTech={'rrrr'} deployLink='aaaa' githubLink={'rrrrr'} />
        </>
    );
};

export default ProjectsCards;
